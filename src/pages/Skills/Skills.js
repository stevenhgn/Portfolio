import React from "react";
import styled from "styled-components";
import { spacing, palette } from "@material-ui/system";
const Skills = () => {
  return <Wrapper>Skills {"&"} experience</Wrapper>;
};

const Wrapper = styled.div`
  ${spacing}
  ${palette}
`;
export default Skills;
